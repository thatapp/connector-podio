{
  "type": "object",
  "properties": {
    "app_id": {
      "type": "number",
      "required": true,
      "title": "App Id"
    },
    "sort_by": {
      "type": "string",
      "title": "Sort By",
      "required": false
    },
    "sort_desc": {
      "type": "boolean",
      "enum": [
        true,
        false
      ],
      "enumTitles": [
        "True",
        "False"
      ],
      "title": "Sort Descending",
      "required": false
    },
    "limit": {
      "type": "number",
      "title": "Batch Size",
      "required": false,
      "description": "Number of items per batch (default: 15)"
    },
    "filters": {
      "type": "object",
      "title": "Filters",
      "required": false,
      "description": "Filter criteria as JSON object with field_id/external_id as keys"
    }
  }
}
